version: "3"
services:
  api:
    build: 
      context: ./
      dockerfile: api.dockerfile
      args:
        env: local
        app_dir: /usr/src/littlebid-api
    image: xanderza/littlebid-api:latest
    ports:
      - "4003:3333"
      - "446:443"
    depends_on:
      - redis
    volumes:
      - /var/www/littlebid-api:/usr/src/littlebid-api
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.2"
          memory: 128M
      restart_policy:
        condition: on-failure
    networks:
      - webnet
  redis:
    image: redis
    ports:
      - "6377:6379"
    volumes:
      - /home/docker/data:/data
    deploy:
      placement:
        constraints: [node.role == manager]
    command: redis-server --appendonly yes
    networks:
      - webnet
networks:
  webnet: